import { View, Text } from "react-native";

function Game() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Game!</Text>
    </View>
  );
}

export default Game;
